services:
  - type: web
    name: the-5-designs
    env: node
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: data-disk
      mountPath: /opt/render/project/src/data
      sizeGB: 1
    envVars:
      - key: NODE_VERSION
        value: ~18.0.0
